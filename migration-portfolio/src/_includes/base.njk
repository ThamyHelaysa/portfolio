<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>{{ title or "My Portfolio" }}</title>

  {% if description %}
    <meta name="description" content="{{ description }}">
  {% endif %}

  <!-- OpenGraph -->
  <meta property="og:title" content="{{ title or "My Portfolio" }}">
  {% if description %}
    <meta property="og:description" content="{{ description }}">
  {% endif %}
  <meta property="og:type" content="website" />

</head>
<body>
  <header>
    <nav>
      <a href="/">Home</a> |
      <a href="/about/">About</a> |
      <a href="/work/">Work</a> |
      <a href="/blog/">Blog</a>
    </nav>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <p>&copy; {{ now | date("yyyy") }} My Name</p>
  </footer>
</body>
</html>
