{% extends "./base.njk" %}

{% block content %}
  <article>
    <h1>{{ title }}</h1>

    {% if date %}
      <p><small>{{ date }}</small></p>
    {% endif %}
    <div>
      {{ content | safe }}
    </div>
  </article>
{% endblock %}
